{
  "db": {
    "name": "db",
    "connector": "postgresql",
    "host": "${SEARCH_API_DB_HOST}",
    "port": "${SEARCH_API_DB_PORT}",
    "database": "${SEARCH_API_DB_NAME}",
    "password": "${SEARCH_API_DB_PASSWORD}",
    "user": "${SEARCH_API_DB_USER}",
    "schema": "${SEARCH_API_DB_SCHEMA}",
    "ssl": false
  }
}
